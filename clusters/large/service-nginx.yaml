apiVersion: v1
kind: Service
metadata:
  name: mastodon-nginx
  namespace: mastodon
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    name: http
  selector:
    app.kubernetes.io/name: nginx
