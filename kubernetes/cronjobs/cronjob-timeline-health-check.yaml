apiVersion: batch/v1
kind: CronJob
metadata:
  name: timeline-health-check
  namespace: mastodon
spec:
  # Run every 5 minutes
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      # Jobs should complete within 2 minutes
      activeDeadlineSeconds: 120
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
          containers:
            - name: timeline-health
              # TODO: Update with actual image registry path
              image: ghcr.io/five-borough-fedi-project/masto.nyc-docean/timeline-health:f7bad317ef0e142c5633ca2fc57d0108b8cb1911
              imagePullPolicy: IfNotPresent
              resources:
                requests:
                  memory: "64Mi"
                  cpu: "50m"
                limits:
                  memory: "128Mi"
                  cpu: "100m"
              envFrom:
                # ConfigMap contains MASTODON_HOSTNAME, FRESHNESS_THRESHOLD
                - configMapRef:
                    name: timeline-health-config
