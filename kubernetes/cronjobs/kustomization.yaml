# Scheduled maintenance. pg-repack-statuses is deliberately absent: it is
# commented out end to end and needs a one-time CREATE EXTENSION before it
# can run. Add it here when that is done.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mastodon
resources:
  - cronjob-media-prune-profiles.yaml
  - cronjob-media-remove-orphans.yaml
  - cronjob-media-remove.yaml
  - cronjob-migration-status.yaml
  - cronjob-postgres-backup.yaml
  - cronjob-preview-cards-remove.yaml
  - cronjob-statuses-remove.yaml
  - cronjob-sync-blocked-email-domains.yaml
  - cronjob-timeline-health-check.yaml
