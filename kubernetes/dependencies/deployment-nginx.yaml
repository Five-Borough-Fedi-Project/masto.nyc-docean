apiVersion: apps/v1
kind: Deployment
metadata:
  name: mastodon-nginx
  namespace: mastodon
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: nginx
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nginx
    spec:
      volumes:
        - name: masto-nginx-config
          configMap:
            name: masto-nginx-config-prd
        - name: page-replica-cache
          emptyDir: {}
      containers:
        - name: mastodon-nginx
          image: docker.io/nginx:1.29.1-alpine-slim
          ports:
          - containerPort: 80
            name: http
          volumeMounts:
            - name: masto-nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
            - name: page-replica-cache
              mountPath: /tmp/page-replica
        - name: page-replica
          image: ghcr.io/five-borough-fedi-project/masto.nyc-docean/page-replica:2e89a9058af7e993a49f30680d18835923e1f5a566f172248b0af4b9aac58df5    
          imagePullPolicy: Always
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
          ports:
            - containerPort: 8080
          env:
            - name: BASE_URL
              value: "https://masto.nyc"
            - name: REMOVE_JS
              value: "true"
            - name: ADD_BASE_URL
              value: "true"
            - name: CACHE_FOLDER
              value: "/tmp/page-replica"
            - name: RESTRICT_TO_BASE_URL
              value: "true"
          volumeMounts:
            - name: page-replica-cache
              mountPath: /tmp/page-replica